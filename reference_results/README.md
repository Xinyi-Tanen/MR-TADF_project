# Reference results

This directory contains prediction and screening outputs supplied with the
project:

- `all_predictions_summary.csv`;
- `screened_candidates.xlsx`;
- `scaffold_based_selected_molecules.xlsx`.

They are retained separately from `data/` because they are derived outputs, not
model inputs. Newly generated outputs are written to `results/`.
